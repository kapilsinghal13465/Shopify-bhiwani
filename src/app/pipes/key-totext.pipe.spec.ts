import { KeyTotextPipe } from './key-totext.pipe';

describe('KeyTotextPipe', () => {
  it('create an instance', () => {
    const pipe = new KeyTotextPipe();
    expect(pipe).toBeTruthy();
  });
});
